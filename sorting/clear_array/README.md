Given an array, remove all the elements from the array.

Constraint : cost of removing an element = sum of array elements before removal

Example:
```buildoutcfg
Input:
    arr = [2,3,1,4]
Output:
    arr = []
```